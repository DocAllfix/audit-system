[pytest]
testpaths = tests
markers =
    benchmark: test di performance/regression su set reali (skip automatico se asset mancanti)
    integration: test che richiedono GEMINI_API_KEY o servizi esterni (skip automatico se mancanti)
filterwarnings =
    ignore::DeprecationWarning:google.*
