# =============================================================================
# AUDIT-OS FRONTEND — .env.example
# Copia in .env e compila con i valori reali (NON committare .env)
# =============================================================================

# URL del backend FastAPI (default: http://localhost:8000 per sviluppo locale)
VITE_API_BASE_URL=http://localhost:8000
